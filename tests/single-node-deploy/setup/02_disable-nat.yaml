- name: disable internet access (enforce airgap)
  node: host
  step:
    type: execute
    options:
      command: lxc network set sn-deploy-net ipv4.nat false

# Apt-sources strip removed. The pinned onramp branch (airgap-integration)
# carries PR #180 which detects airgap via a probe and skips
# `update_cache` accordingly. Leave the VM's stock sources in place so
# the test exercises that probe end-to-end.
